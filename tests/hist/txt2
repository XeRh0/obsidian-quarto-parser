> [!code] Vyber jména a adresy kin, která hrají alespoň tolik filmů jako kino Golden Apple Cinema
> \`\`\`sql
> SELECT distinct nazev, mesto, ulice, cislo_popisne
> FROM kina k
> WHERE (
>     SELECT COUNT (id_kina)
>     FROM predstaveni p
>     WHERE p.id_kina = k.id_kina
>     ) >= (
>     select count(*)
>     from predstaveni
>     where id_kina = (
>         select id_kina
>         from kina
>         where nazev = ’Golden Apple Cinema’
>         )
>     );
> \`\`\`
